#include <mega32.h>
#include <delay.h>


int i=0;
unsigned char seg[10] = {
    0x3F,0x06,0x5B,0x4F,0x66,
    0x6D,0x7D,0x07,0x7F,0x6F
};

void main(void)
{
    DDRB = 0xFF;   // PORTB ?????
    PORTB = 0x00;

    while (1)
    {
        for (i = 0; i < 10; i++)
        {
            PORTB = seg[i];  // ????? ???
            delay_ms(500);
        }
    }
}
